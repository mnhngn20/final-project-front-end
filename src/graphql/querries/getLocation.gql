query getLocation($id: Float!) {
  getLocation(id: $id) {
    message
    location {
      id
      name
      address
      long
      lat
      images
      thumbnail
      description
      numOfFloor
      income
      isActive
      createdAt
      minPrice
      electricCounterPrice
      locationServices {
        id
        name
        description
      }
      totalRevenue
      contactInformations {
        address
        name
        id
        phoneNumber
        email
      }
      users {
        id
      }
    }
  }
}
